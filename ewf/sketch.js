// Project Title
// Your Name
// Date
//
// Extra for Experts:
// - describe what you did to take this project "above and beyond"

let fillMain = (0, 0, 0);
let slider;

function setup() {
  createCanvas(windowWidth, windowHeight);
  
}

function draw() {
  background(220);

  rectMode(CENTER);

  fill(fillMain);
  rect(width/2, height-height/4, 200, 100);
}

